<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.WildcatsDen"
        android:networkSecurityConfig="@xml/network_security_config"

        tools:targetApi="31">
        <activity
            android:name=".auth.components.SignUpModal"
            android:exported="false" />
        <activity
            android:name=".auth.components.SignInModal"
            android:exported="false" />

        <activity
            android:name=".core.network.components.Header"
            android:exported="false" />

        <activity
            android:name=".venues.VenuesGridActivity"
            android:exported="false"
            android:parentActivityName=".core.network.components.HomePage" />

        <activity
            android:name=".venues.VenueDetailsActivity"
            android:exported="false"
            android:parentActivityName=".venues.VenuesGridActivity" />

        <activity
            android:name=".venues.VenueOverviewActivity"
            android:exported="false"
            android:parentActivityName=".core.network.components.HomePage" />


        <activity
            android:name=".core.network.components.HomePage"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>