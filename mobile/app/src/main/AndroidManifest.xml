<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WildcatsDen"
        android:networkSecurityConfig="@xml/network_security_config"

        tools:targetApi="31">
        <activity
            android:name=".SignUpModal"
            android:exported="false" />
        <activity
            android:name=".SignInModal"
            android:exported="false" />
        <activity
            android:name=".VenuesActivity"
            android:exported="false" />
        <activity
            android:name=".Header"
            android:exported="false" />

        <activity
            android:name=".VenuesGridActivity"
            android:exported="false"
            android:parentActivityName=".HomePage" />

        <activity
            android:name=".VenueDetailsActivity"
            android:exported="false"
            android:parentActivityName=".VenuesGridActivity" />

        <activity
            android:name=".VenueOverviewActivity"
            android:exported="false"
            android:parentActivityName=".HomePage" />


        <activity
            android:name=".HomePage"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>